# Firebase config is loaded from firebase-applet-config.json.

# Optional: override authDomain for custom-domain deployments.
# Set this to the hostname where the app is served, then add that same
# domain to Firebase Console → Authentication → Settings → Authorized domains.
# VITE_FIREBASE_AUTH_DOMAIN=your-custom-domain.example.com
